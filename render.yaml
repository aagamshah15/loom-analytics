services:
  - type: web
    name: loom-api
    env: python
    buildCommand: pip install -r requirements.txt && pip install -e .
    startCommand: uvicorn pipeline.api.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
